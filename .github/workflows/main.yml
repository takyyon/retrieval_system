name: CI

on:
  pull_request:
    paths: '**.md'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/first-interaction@v1
        with:
          pr-message: 'This is a pr message'
